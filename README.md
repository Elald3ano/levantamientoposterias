# PosteApp

Aplicación web progresiva (PWA) para levantamiento de postes en campo. Funciona offline, toma fotos georreferenciadas con marca de agua, y exporta los datos en CSV + KML + fotos.

---

## Funcionalidades

- **Acceso por código**: el administrador crea proyectos con código de acceso único. Los técnicos ingresan con ese código.
- **Registro de postes**: formulario con campos de identificación, características físicas, coordenada GPS y 3 fotos.
- **GPS integrado**: captura coordenadas con precisión, geocodificación inversa (dirección), y mapa satelital para ajuste fino.
- **Fotos con marca de agua**: cada foto se sella con coordenadas, fecha, hora, poste y proyecto.
- **Modo libre vs estricto**: libre sin restricciones; estricto obliga GPS + 3 fotos + todos los campos.
- **Auto-persistencia de características**: material, altura, carga y tensión se conservan entre postes consecutivos.
- **Exportación ZIP**: descarga un archivo con `CSV` + `KML` + carpeta de `fotos`.
- **Offline-first**: guarda localmente sin conexión, sincroniza con Supabase al reconectar.
- **Código admin configurable**: el código de administrador se guarda en Supabase y se cambia desde la app.

---

## Cómo usar

### Administrador

1. Ingresa con el código de administrador (por defecto `4839`).
2. Crea un proyecto: nombre, código de acceso (6 caracteres) y modo (libre o estricto).
3. Comparte el código de acceso con los técnicos.
4. Desde la pestaña **Exportar** puedes descargar el ZIP completo.

### Técnico

1. Ingresa el código de acceso del proyecto.
2. Completa los campos del poste en la pestaña **Encuesta**.
3. Captura la coordenada GPS en la pestaña **Ubicación**.
4. Toma 3 fotos en la pestaña **Fotos** (requiere GPS previo).
5. Guarda con el botón ✓ al final de la pestaña Fotos.
6. El siguiente poste hereda material, altura, carga y tensión del anterior.

### Modo estricto

En modo estricto es obligatorio llenar:
- N° Poste, Dirección, Material, Altura, Carga, Tensión, Estado
- Coordenada GPS
- 3 fotos

No se puede guardar sin estos datos.

---

## Despliegue

### Requisitos

- Repositorio en GitHub
- Proyecto en [Supabase](https://supabase.com) (gratuito)

### Supabase — Base de datos

Ejecutar en el **SQL Editor** de Supabase:

```sql
-- Tabla de configuración (código admin, etc.)
CREATE TABLE IF NOT EXISTS config (
  key   TEXT PRIMARY KEY,
  value TEXT
);

-- Tabla de proyectos
CREATE TABLE IF NOT EXISTS proyectos (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  nombre        TEXT NOT NULL,
  proyecto_key  TEXT UNIQUE NOT NULL,
  codigo_acceso TEXT NOT NULL,
  modo          TEXT DEFAULT 'libre',
  activo        BOOLEAN DEFAULT true,
  creado        TIMESTAMPTZ DEFAULT now()
);

-- Tabla de registros (postes)
CREATE TABLE IF NOT EXISTS registros (
  id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  proyecto   TEXT NOT NULL,
  seq        INTEGER,
  uid        TEXT,
  fecha      TEXT,
  hora       TEXT,
  n_poste    TEXT,
  direccion  TEXT,
  material   TEXT,
  altura     TEXT,
  carga      TEXT,
  tension    TEXT,
  estado     TEXT,
  lat        TEXT,
  lng        TEXT,
  fotos      JSONB DEFAULT '[]',
  equipamiento JSONB DEFAULT '{}',
  obs        TEXT,
  creado     TIMESTAMPTZ DEFAULT now()
);

-- Permitir operaciones anónimas (app usa anon key)
ALTER TABLE config ENABLE ROW LEVEL SECURITY;
ALTER TABLE proyectos ENABLE ROW LEVEL SECURITY;
ALTER TABLE registros ENABLE ROW LEVEL SECURITY;

CREATE POLICY "anon_manage_config" ON config FOR ALL TO anon USING (true) WITH CHECK (true);
CREATE POLICY "anon_manage_proyectos" ON proyectos FOR ALL TO anon USING (true) WITH CHECK (true);
CREATE POLICY "anon_manage_registros" ON registros FOR ALL TO anon USING (true) WITH CHECK (true);
```

### Supabase — Storage

Crear un bucket público llamado `fotos`. Las políticas deben permitir lectura/escritura anónima.

### GitHub Pages

1. Sube `index.html` a la rama `master` o `main`.
2. En Settings → Pages, selecciona la rama y carpeta raíz.
3. La app estará disponible en `https://tuusuario.github.io/repo/`.

---

## Estructura del proyecto

```
levantamientoposterias/
├── index.html    # Toda la app (HTML + CSS + JS inline, ~2000 líneas)
└── README.md     # Este archivo
```

No hay dependencias de compilación. Las librerías externas se cargan por CDN:
- [Leaflet](https://leafletjs.com/) — mapa interactivo
- [JSZip](https://stuk.github.io/jszip/) — generación de archivos ZIP
- [Supabase JS](https://supabase.com/docs/reference/javascript) — backend
- Google Fonts: Inter + JetBrains Mono

---

## Autor

**Daniel Cordoba** — Cali, Colombia  
WhatsApp: [+57 3166371216](https://wa.me/573166371216)  
Email: [danico8822@gmail.com](mailto:danico8822@gmail.com)
